import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hadestech.phasercny',
  appName: 'PhaserCNY',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
